# Extra Scripts for the AWS VPC NetBox Plugin

These scripts are meant to serve as functional examples of how you might
use the AWS VPC NetBox plugin.

The intial example script allows you to discover a given AWS VPC and add it to NetBox
using both the custom models and built-in NetBox resources (namely Prefix).
